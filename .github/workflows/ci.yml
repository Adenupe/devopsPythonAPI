name: CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Install dependencies
        run:  pip install -r requirements.txt

      - name: Check app imports
        run:  python -c "import app.main"

      #- name: login to docker hub
      #  run:  echo "${{secrets.DOCKER_PASSWORD}}" | docker login -u "${{secrets.DOCKER_USERNAME}}" --password-stdin

      - name: Build Docker image
        run:  docker build -t ${{secrets.DOCKER_USERNAME}}/fastapi-app:latest .

      - name: Push docker image to dockerhub
        run:  docker push ${{secrets.DOCKER_USERNAME}}/fastapi-app:latest

#the lines below are not needed when deploying to the cloud. You only need to build the image and push to the cloud
#      - name: create env file
#        run: |
#          echo "APP_NAME= ${{secrets.APP_NAME}}" > .env

#      - name: docker compose run
#        run: docker compose up -d

#      - name: check docker logs
#        run: docker compose ps
